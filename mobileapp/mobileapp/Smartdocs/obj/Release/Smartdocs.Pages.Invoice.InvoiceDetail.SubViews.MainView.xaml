<?xml version="1.0" encoding="UTF-8"?>
<ContentView
	xmlns="http://xamarin.com/schemas/2014/forms"
	xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
	x:Class="Smartdocs.MainView"
	BackgroundColor="#f2f9fc">
    <Grid Padding="10, 10, 10, 10" RowSpacing="0">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
            <!--<RowDefinition Height="90"></RowDefinition>-->
        </Grid.RowDefinitions>

        <StackLayout HorizontalOptions="FillAndExpand" BackgroundColor="#0d73a2" Grid.Row="0" HeightRequest="5"></StackLayout>

        <Grid Grid.Row="1"  VerticalOptions="FillAndExpand"  HorizontalOptions="FillAndExpand" >
            <Grid.RowDefinitions>
                <RowDefinition Height="45*"></RowDefinition>
            </Grid.RowDefinitions>
            <StackLayout HorizontalOptions="FillAndExpand" VerticalOptions="FillAndExpand" Grid.Row="0">
                <ScrollView  x:Name="GridRow" VerticalOptions="FillAndExpand">
                    <StackLayout HorizontalOptions="FillAndExpand" VerticalOptions="FillAndExpand">
                        <StackLayout x:Name="Row" VerticalOptions="FillAndExpand"></StackLayout>
                    </StackLayout>
                </ScrollView>
            </StackLayout>
        </Grid>

     
    </Grid>
</ContentView>